<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document Management - Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
    <script src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              'admin-bg': '#0f172a',
              'admin-card': '#1e293b',
              'admin-border': '#334155',
              'admin-accent': '#3b82f6',
              'admin-text': '#f1f5f9',
              'admin-text-dim': '#94a3b8',
            }
          }
        }
      }
    </script>
</head>
<body class="bg-admin-bg text-admin-text min-h-screen">
    <!-- Header -->
    <header class="bg-admin-card border-b border-admin-border px-6 py-4">
        <div class="flex items-center justify-between">
            <h1 class="text-2xl font-bold text-admin-text flex items-center">
                <i class="fas fa-file-alt text-admin-accent mr-3"></i>
                Document Management
            </h1>
            <div class="flex items-center space-x-4">
                <a href="/admin" class="text-admin-text-dim hover:text-admin-text">
                    <i class="fas fa-arrow-left mr-2"></i>Back to Dashboard
                </a>
                <a href="/logout" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg">
                    <i class="fas fa-sign-out-alt mr-2"></i>Logout
                </a>
            </div>
        </div>
    </header>

    <!-- Navigation -->
    <nav class="bg-admin-card border-b border-admin-border px-6 py-3">
        <div class="flex space-x-6">
            <a href="/admin" class="text-admin-text-dim hover:text-admin-text">
                <i class="fas fa-chart-bar mr-2"></i>Dashboard
            </a>
            <a href="/admin/users" class="text-admin-text-dim hover:text-admin-text">
                <i class="fas fa-users mr-2"></i>Users
            </a>
            <a href="/admin/documents" class="text-admin-accent border-b-2 border-admin-accent pb-2">
                <i class="fas fa-file-alt mr-2"></i>Documents
            </a>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="p-6">
        <!-- Search and Stats -->
        <div class="mb-6 flex flex-col md:flex-row md:items-center md:justify-between space-y-4 md:space-y-0">
            <div>
                <h2 class="text-xl font-semibold text-admin-text">All Documents (<%= total %>)</h2>
                <p class="text-admin-text-dim">Manage all user documents and content</p>
            </div>
            
            <form class="flex space-x-2" method="GET">
                <input type="text" name="search" value="<%= searchQuery %>" 
                       placeholder="Search documents..." 
                       class="bg-admin-card border border-admin-border rounded-lg px-4 py-2 text-admin-text placeholder-admin-text-dim focus:border-admin-accent focus:outline-none">
                <button type="submit" class="bg-admin-accent hover:bg-blue-600 text-white px-4 py-2 rounded-lg">
                    <i class="fas fa-search"></i>
                </button>
            </form>
        </div>

        <!-- Documents Table -->
        <div class="bg-admin-card border border-admin-border rounded-lg overflow-hidden">
            <table class="w-full">
                <thead class="bg-admin-bg">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-admin-text-dim uppercase tracking-wider">Document</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-admin-text-dim uppercase tracking-wider">Author</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-admin-text-dim uppercase tracking-wider">Privacy</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-admin-text-dim uppercase tracking-wider">Created</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-admin-text-dim uppercase tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-admin-border">
                    <% documents.forEach(doc => { %>
                    <tr class="hover:bg-admin-bg">
                        <td class="px-6 py-4">
                            <div>
                                <div class="text-sm font-medium text-admin-text">
                                    <a href="/view/<%= doc._id %>" class="hover:text-admin-accent" target="_blank">
                                        <%= doc.title %>
                                    </a>
                                </div>
                                <div class="text-sm text-admin-text-dim truncate max-w-xs">
                                    <%- doc.content.replace(/<[^>]*>/g, '').substring(0, 100) %>...
                                </div>
                            </div>
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap">
                            <div class="text-sm text-admin-text"><%= doc.user.name %></div>
                            <div class="text-sm text-admin-text-dim"><%= doc.user.email %></div>
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap">
                            <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full <%= doc.isPublic ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
                                <%= doc.isPublic ? 'Public' : 'Private' %>
                            </span>
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-admin-text-dim">
                            <%= new Date(doc.Currentdate).toLocaleDateString() %>
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm space-x-2">
                            <a href="/view/<%= doc._id %>" target="_blank" class="text-blue-400 hover:text-blue-300" title="View">
                                <i class="fas fa-eye"></i>
                            </a>
                            <form method="POST" action="/admin/documents/<%= doc._id %>/toggle-privacy" class="inline">
                                <button type="submit" class="text-yellow-400 hover:text-yellow-300" title="Toggle Privacy">
                                    <i class="fas fa-<%= doc.isPublic ? 'lock' : 'unlock' %>"></i>
                                </button>
                            </form>
                            <a href="/admin/documents/<%= doc._id %>/delete" 
                               onclick="return confirm('Are you sure you want to delete this document?')"
                               class="text-red-400 hover:text-red-300" title="Delete">
                                <i class="fas fa-trash"></i>
                            </a>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>

        <!-- Pagination -->
        <% if (totalPages > 1) { %>
        <div class="mt-6 flex items-center justify-between">
            <div class="text-sm text-admin-text-dim">
                Page <%= currentPage %> of <%= totalPages %>
            </div>
            <div class="flex space-x-2">
                <% if (currentPage > 1) { %>
                <a href="?page=<%= currentPage - 1 %>&search=<%= searchQuery %>" 
                   class="bg-admin-card border border-admin-border text-admin-text px-3 py-2 rounded-lg hover:bg-admin-bg">
                    Previous
                </a>
                <% } %>
                <% if (currentPage < totalPages) { %>
                <a href="?page=<%= currentPage + 1 %>&search=<%= searchQuery %>" 
                   class="bg-admin-accent text-white px-3 py-2 rounded-lg hover:bg-blue-600">
                    Next
                </a>
                <% } %>
            </div>
        </div>
        <% } %>
    </main>

    <!-- Flash Messages -->
    <script>
      window.flashMessages = {
        success: '<%= typeof success !== "undefined" ? success : "" %>',
        error: '<%= typeof error !== "undefined" ? error : "" %>'
      };
    </script>

    <script>
      if (window.flashMessages.success) {
        Toastify({
          text: window.flashMessages.success,
          duration: 3000,
          style: { background: "linear-gradient(to right, #00b09b, #96c93d)" }
        }).showToast();
      }
      
      if (window.flashMessages.error) {
        Toastify({
          text: window.flashMessages.error,
          duration: 3000,
          style: { background: "linear-gradient(to right, #ff5f6d, #ffc371)" }
        }).showToast();
      }
    </script>
</body>
</html>
